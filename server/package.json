{
  "name": "text-to-image-server",
  "version": "1.0.0",
  "description": "Backend for Text to Image Generator",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "npx --yes kill-port 4000 && node server.js",
    "dev": "npx --yes kill-port 4000 && nodemon server.js"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "bcrypt": "^5.1.1",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.0.0",
    "razorpay": "^2.9.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
